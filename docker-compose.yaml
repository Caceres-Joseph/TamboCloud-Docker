version: '2'
services:
  
  proxy:
    build: Proxy
    ports:
      - "80:80"
    depends_on :
      - static
  flask:
    build: Flask
    depends_on:
     - redis
     - static
  redis:
    image: redis
  
